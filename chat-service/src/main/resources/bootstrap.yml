spring:
  application:
    name: chat-service
  profiles:
    active:
      default,dev
  cloud:
    config:
      uri: http://config.logiweb.site/

server:
  port: 8090
  error:
    whitelabel:
      enabled: false
