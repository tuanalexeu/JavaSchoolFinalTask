server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: ${HOME}/Desktop/final-task/config-service/src/main/resources/config/
          search-paths: '{application}'