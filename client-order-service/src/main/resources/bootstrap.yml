spring:
  application:
    name: client-order-service
  profiles:
    active:
      default,dev
  cloud:
    config:
      uri: http://config.logiweb.site/

server:
  port: 8088
  error:
    whitelabel:
      enabled: false