spring:
  application:
    name: logiweb-service
  profiles:
    active:
      default,dev
  cloud:
    config:
      uri: http://config.logiweb.site/

server:
  port: 8080
  error:
    whitelabel:
      enabled: false